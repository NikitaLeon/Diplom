# Планирование автоматизации тестирования
## Объект тестирования: 
### Веб-сервис, который предлагает купить тур по определённой цене двумя способами:
1. Обычная оплата по дебетовой карте.
2. Уникальная технология: выдача кредита по данным банковской карты.
   Приложение в собственной СУБД должно сохранять информацию о том, успешно ли был совершён платёж и каким способом.
#### Тестовые данные:
- валидный номер карты: 4444 4444 4444 4441, статус: APPROVED;
- не валидный номер карты: 4444 4444 4444 4442, статус: DECLINED.
#### Тестируемые СУБД:
- MySQL;
- PostgreSQL.
## Перечень автоматизируемых сценариев:
### Предусловие (для всех сценариев):
#### Открыть [страницу веб-сервиса](http://localhost:8080/).
## Позитивные сценарии:
1. **Оплата по карте (с валидным номером):**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Номер карты` валидными данными;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- в верхнем правом углу появляется сообщение `Успешно`;
- данные могут быть просмотрены в СУБД;
- в СУБД в разделе `payment_entity` появилась запись со статусом `APPROVED`.
2. **Кредит по данным карты (с валидным номером):**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Номер карты` валидными данными;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- в верхнем правом углу появляется сообщение `Успешно`;
- данные могут быть просмотрены в СУБД.
- в СУБД в разделе `credit_request_entity` появилась запись со статусом `APPROVED`.
3. **Оплата по карте (с не валидным номером):**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Номер карты` не валидными данными;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- в верхнем правом углу появляется сообщение `Ошибка`;
- данные могут быть просмотрены в СУБД;
- в СУБД в разделе `payment_entity` появилась запись со статусом `DECLINED`.
4. **Кредит по данным карты (с не валидным номером):**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Номер карты` не валидными данными;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- в верхнем правом углу появляется сообщение `Ошибка`;
- данные могут быть просмотрены в СУБД.
- в СУБД в разделе `credit_request_entity` появилась запись со статусом `DECLINED`.
## Негативные сценарии:
1. **Отправка пустой формы при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:

- под полем `Номер карты` появится сообщение `Неверный формат`;
- под полем `Месяц` появится сообщение `Неверный формат`;
- под полем `Год` появится сообщение `Неверный формат`;
- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`;
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
2. **Отправка пустой формы при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Номер карты` появится сообщение `Неверный формат`;
- под полем `Месяц` появится сообщение `Неверный формат`;
- под полем `Год` появится сообщение `Неверный формат`;
- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`;
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
3. **Отправка формы без номера карты при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- оставить поле `Номер карты` пустым;
- остальные поля заполнить валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Номер карты` появится сообщение `Неверный формат`.
4. **Отправка формы без номера карты при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- оставить поле `Номер карты` пустым;
- остальные поля заполнить валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Номер карты` появится сообщение `Неверный формат`.
5. **Отправка формы с неполным номером карты при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Номер карты` неполными данными (`4444 4444 4444 444`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Номер карты` появится сообщение `Неверный формат`.
6. **Отправка формы с неполным номером карты при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Номер карты` неполными данными (`4444 4444 4444 444`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Номер карты` появится сообщение `Неверный формат`.
7. **Отправка формы с пустым полем `Месяц` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- оставить поле `Месяц` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверный формат`.
8. **Отправка формы с пустым полем `Месяц` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- оставить поле `Месяц` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверный формат`.
9. **Отправка формы со значением в поле `Месяц` (`текущий месяц - 1`) при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Месяц` данными на месяц (`текущий месяц - 1`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
10. **Отправка формы со значением в поле `Месяц` (`текущий месяц - 1`) при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Месяц` данными (`текущий месяц - 1`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
11. **Отправка формы со значением в поле `Месяц` ниже граничного при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Месяц` данными `00`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
12. **Отправка формы со значением в поле `Месяц` ниже граничного при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Месяц` данными `00`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
13. **Отправка формы со значением в поле `Месяц` выше граничного при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Месяц` данными `13`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
14. **Отправка формы со значением в поле `Месяц` выше граничного при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Месяц` данными `13`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Месяц` появится сообщение `Неверно указан срок действия карты`.
15. **Отправка формы с пустым полем `Год` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- оставить поле `Год` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Неверный формат`.
16. **Отправка формы с пустым полем `Год` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- оставить поле `Год` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Неверный формат`.
17. **Отправка формы со значением в поле `Год` (`текущий год - 1`) при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Год` данными на месяц (`текущий год - 1`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Истёк срок действия карты`.
18. **Отправка формы со значением в поле `Год` (`текущий год - 1`) при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Год` данными на месяц (`текущий год - 1`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Истёк срок действия карты`.
19. **Отправка формы со значением в поле `Год` (`текущий год + 6`) при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `Год` данными на месяц (`текущий год + 6`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Неверно указан срок действия карты`.
20. **Отправка формы со значением в поле `Год` (`текущий год + 6`) при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `Год` данными на месяц (`текущий год + 6`);
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Год` появится сообщение `Неверно указан срок действия карты`.
21. **Отправка формы с пустым полем `Владелец` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- оставить поле `Владелец` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`.
22. **Отправка формы с пустым полем `Владелец` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- оставить поле `Владелец` пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`.
23. **Отправка формы с пробелом в поле `Владелец` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- в поле `Владелец` ввести `пробел`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:

- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`.
24. **Отправка формы с пробелом в поле `Владелец` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- в поле `Владелец` ввести `пробел`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Поле обязательно для заполнения`.
25. **Отправка формы со спецсимволами в поле `Владелец` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- в поле `Владелец` ввести `любой спецсимвол`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Неверный формат имени владельца`.
26. **Отправка формы со спецсимволами в поле `Владелец` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- в поле `Владелец` ввести `любой спецсимвол`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Неверный формат имени владельца`.
27. **Отправка формы с числовым значением в поле `Владелец` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- в поле `Владелец` ввести `любое число`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Неверный формат имени владельца`.
28. **Отправка формы с числовым значением в поле `Владелец` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- в поле `Владелец` ввести `любое число`;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `Владелец` появится сообщение `Неверный формат имени владельца`.
29. **Отправка формы с пустым полем `CVC/CVV` при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- поле `CVC/CVV` оставить пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
30. **Отправка формы с пустым полем `CVC/CVV` при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- поле `CVC/CVV` оставить пустым;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
31. **Отправка формы с неполным номером CVC/CVV при оплате картой:**
- на странице веб-сервиса кликнуть кнопку `Купить`;
- заполнить поле `CVC/CVV` данными из 2-ух чисел;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
32. **Отправка формы с неполным номером CVC/CVV при покупке в кредит:**
- на странице веб-сервиса кликнуть кнопку `Купить в кредит`;
- заполнить поле `CVC/CVV` данными из 2-ух чисел;
- заполнить остальные поля валидными данными;
- кликнуть кнопку `Продолжить`.
#### Ожидаемый результат:
- под полем `CVC/CVV` появится сообщение `Неверный формат`.
## Перечень используемых инструментов:
1. `IntelliJ IDEA` - интегрированная среда разработки программного обеспечения для многих языков программирования.
2. `Java 11` - строго типизированный объектно-ориентированный язык программирования общего назначения.
3. `Gradle` - система автоматизации сборки для проведения автоматизации.
4. `JUnit5` - платформа для написания автотестов.
5. `Selenide` - фреймворк для автоматизированного тестирования веб-приложений.
6. `Facer` - библиотека, которая позволяет генерировать случайные данные.
7. `MySQL` - реляционная система управления базами данных (СУБД), предназначена для работы с базами реляционного типа.
8. `PostgreSQL` - объектно-реляционная система управления базами данных (СУБД) с открытым исходным кодом. Она помогает хранить информацию и организовывать её.
9. `DBeaver` - приложение для работы с различными базами данных.
10. `Allure` - инструмент для создания отчётов о тестировании.
11. `Docker` - программная платформа для разработки, доставки и запуска контейнерных приложений.
## Перечень и описание возможных рисков при автоматизации:
- Отсутствие технической документации.
- Возможны проблемы из-за необходимости поддержки двух СУБД (MySQL, PostgreSql).
- Переполнение базы данных ненужной информацией.
- Ложные отправки форм увеличивают нагрузку на систему.
## Интервальная оценка с учётом рисков в часах:
В среднем составит 120 часов.
## План сдачи работ: когда будут готовы автотесты, результаты их прогона:
- Автоматизация тестирования: 25.02.2025.
- Документация по проведению автоматизации: 28.02.2025.









